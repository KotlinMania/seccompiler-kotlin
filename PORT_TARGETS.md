# Port targets — `seccompiler-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `seccompiler` that codex-rs uses, and therefore the minimum subset that `seccompiler-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-125] (rank `84`, tier `direct`, status `To Do`)

**Cargo package name**: `seccompiler`
**Rust module name (in `use` statements)**: `seccompiler`

## Summary

- **9** distinct symbols imported by codex-rs
- **9** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `seccompiler::BpfProgram`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:24` (`codex-linux-sandbox`) — `use seccompiler::BpfProgram`

### `seccompiler::SeccompAction`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:25` (`codex-linux-sandbox`) — `use seccompiler::SeccompAction`

### `seccompiler::SeccompCmpArgLen`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:26` (`codex-linux-sandbox`) — `use seccompiler::SeccompCmpArgLen`

### `seccompiler::SeccompCmpOp`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:27` (`codex-linux-sandbox`) — `use seccompiler::SeccompCmpOp`

### `seccompiler::SeccompCondition`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:28` (`codex-linux-sandbox`) — `use seccompiler::SeccompCondition`

### `seccompiler::SeccompFilter`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:29` (`codex-linux-sandbox`) — `use seccompiler::SeccompFilter`

### `seccompiler::SeccompRule`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:30` (`codex-linux-sandbox`) — `use seccompiler::SeccompRule`

### `seccompiler::TargetArch`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:31` (`codex-linux-sandbox`) — `use seccompiler::TargetArch`

### `seccompiler::apply_filter`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:32` (`codex-linux-sandbox`) — `use seccompiler::apply_filter`
