# Rust callers — `seccompiler-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `seccompiler` | `seccompiler` | 9 | 1 | 1 | 9 |

## `seccompiler`

### Inbound symbols (9 distinct, 9 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `seccompiler`.

#### `seccompiler::BpfProgram` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:24` — `use seccompiler::BpfProgram`

#### `seccompiler::SeccompAction` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:25` — `use seccompiler::SeccompAction`

#### `seccompiler::SeccompCmpArgLen` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:26` — `use seccompiler::SeccompCmpArgLen`

#### `seccompiler::SeccompCmpOp` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:27` — `use seccompiler::SeccompCmpOp`

#### `seccompiler::SeccompCondition` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:28` — `use seccompiler::SeccompCondition`

#### `seccompiler::SeccompFilter` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:29` — `use seccompiler::SeccompFilter`

#### `seccompiler::SeccompRule` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:30` — `use seccompiler::SeccompRule`

#### `seccompiler::TargetArch` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:31` — `use seccompiler::TargetArch`

#### `seccompiler::apply_filter` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:32` — `use seccompiler::apply_filter`
