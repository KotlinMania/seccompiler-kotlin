# Port report — `seccompiler-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `seccompiler`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 9 `use` sites from 1 other repos
- **Outbound**: 15 `use` sites to 2 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `seccompiler` | `seccompiler` | 9 | 2 | COD-125 | 84 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (9 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-linux-sandbox` | 9 | ✅ |

### Symbol-level demand (9 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `seccompiler::BpfProgram` | 1 | 1 | 0 | 1 |
| `seccompiler::SeccompAction` | 1 | 1 | 0 | 1 |
| `seccompiler::SeccompCmpArgLen` | 1 | 1 | 0 | 1 |
| `seccompiler::SeccompCmpOp` | 1 | 1 | 0 | 1 |
| `seccompiler::SeccompCondition` | 1 | 1 | 0 | 1 |
| `seccompiler::SeccompFilter` | 1 | 1 | 0 | 1 |
| `seccompiler::SeccompRule` | 1 | 1 | 0 | 1 |
| `seccompiler::TargetArch` | 1 | 1 | 0 | 1 |
| `seccompiler::apply_filter` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 9 symbols)

##### `seccompiler::BpfProgram` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:24` — `use seccompiler::BpfProgram`

##### `seccompiler::SeccompAction` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:25` — `use seccompiler::SeccompAction`

##### `seccompiler::SeccompCmpArgLen` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:26` — `use seccompiler::SeccompCmpArgLen`

##### `seccompiler::SeccompCmpOp` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:27` — `use seccompiler::SeccompCmpOp`

##### `seccompiler::SeccompCondition` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:28` — `use seccompiler::SeccompCondition`

##### `seccompiler::SeccompFilter` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:29` — `use seccompiler::SeccompFilter`

##### `seccompiler::SeccompRule` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:30` — `use seccompiler::SeccompRule`

##### `seccompiler::TargetArch` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:31` — `use seccompiler::TargetArch`

##### `seccompiler::apply_filter` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:32` — `use seccompiler::apply_filter`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `serde-kotlin` | 7 | 6 | `serde` | COD-128 | 1 | To Do |
| `libc-kotlin` | 8 | 8 | `libc` | COD-75 | 3 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
